[tool.ty.environment]
python-platform = "all"
root = [".", "tests_61be1f7"]

[tool.ty.rules]
all = "error"

[tool.uv]
add-bounds = "exact"
exclude-newer = "P5D"
package = false
required-version = ">=0.11.0"
