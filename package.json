{
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.1.0",
    "@j178/prek": "^0.4.11",
    "if-ci": "^3.0.0",
    "markdownlint-cli2": "^0.23.1",
    "prettier": "^3.9.6"
  },
  "name": "ledger",
  "packageManager": "bun@1.3.10",
  "private": true,
  "scripts": {
    "build": "bun run check && bun run build:force",
    "build:force": "node -e \"console.log('No build step')\"",
    "check": "bun run check:journal && bun run check:md && bun run check:prettier && bun run check:py",
    "check:journal": "uv run --locked -m scripts.check && uv run --locked -m scripts.format --check",
    "check:md": "markdownlint-cli2",
    "check:prettier": "prettier --cache --check \"**/*.{astro,cjs,css,csv,gql,graphql,hbs,html,js,jsx,json,json5,jsonc,jsonl,less,mjs,pcss,sass,scss,svelte,styl,ts,tsx,vue,xml,yaml,yml}\"",
    "check:py": "uv run --locked ruff check && uv run --locked ruff format --check && uv run --locked ty check",
    "clean": "node -e \"console.log('No clean step')\"",
    "commitlint": "commitlint --from=origin/main --to=HEAD",
    "format": "bun run format:journal && bun run format:md && bun run format:prettier && bun run format:py",
    "format:journal": "uv run --locked -m scripts.check && uv run --locked -m scripts.format",
    "format:md": "markdownlint-cli2 --fix",
    "format:prettier": "prettier --cache --write \"**/*.{astro,cjs,css,csv,gql,graphql,hbs,html,js,jsx,json,json5,jsonc,jsonl,less,mjs,pcss,sass,scss,svelte,styl,ts,tsx,vue,xml,yaml,yml}\"",
    "format:py": "uv run --locked ruff check --fix && uv run --locked ruff format && uv run --locked ty check",
    "hledger:install": "bun run scripts/install-hledger.mjs",
    "prepare": "(uv sync || true) && (bun run hledger:install || true) && bun x --package if-ci -- if-not-ci prek install",
    "test": "bun run test:py",
    "test:py": "uv run --locked pytest"
  },
  "type": "module",
  "workspaces": [
    "."
  ]
}
